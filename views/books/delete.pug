extends ../layout

block content
  h3

    | Are you sure you want to delete 
    em= book.title
    | ?
  form(action=`/books/${book.id}/delete` method="post")

      p
          a.button(href="/books/") No! Take me back! 
      p

          input(type="submit", value = "Yes, I'm sure")
   
   
